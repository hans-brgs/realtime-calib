import { createTheme } from '@mantine/core';

// Aligned with vision-webapp: rely on Mantine's defaults and set dark mode at the
// provider (defaultColorScheme="dark", see App.tsx). Add overrides here as the
// design system evolves.
export const theme = createTheme({});
