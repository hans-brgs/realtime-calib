# Vendored Yarn Berry release (zero global tooling required — matches the
# machine's yarn wrapper, which execs the release pointed to by yarnPath).
yarnPath: .yarn/releases/yarn-4.17.0.cjs

# Use a classic node_modules layout rather than PnP: most compatible with
# Docusaurus and its plugin ecosystem.
nodeLinker: node-modules
